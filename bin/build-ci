#!/usr/bin/env bash

# This command is internal to Betty's own CI setup.

set -Eeuo pipefail

cd "$(dirname "$0")/.."

pip install '.[ci]'
./bin/build-dev-npm

if [ -z "${BETTY_TEST_SKIP_PLAYWRIGHT-}" ]; then
  playwright install
fi

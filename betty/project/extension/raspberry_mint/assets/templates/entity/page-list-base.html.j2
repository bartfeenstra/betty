{% extends 'base.html.j2' %}
{% set page_title = page_title | default(entity_type.label_plural | localize) %}
{% do breadcrumbs.append(
    page_title,
    page_resource | url(absolute=true)
) %}
{% block page_summary %}
    <p>
        {{ entity_type.label_countable.count(entities | length) | localize | html_lang }}
    </p>
{% endblock %}

{% set linked_data=[
    breadcrumbs.dump(app),
] | minimize | none_void %}
{% if linked_data %}
    <script type="application/ld+json">
        {{ linked_data | json }}
    </script>
{% endif %}
{% set linked_data=[
    breadcrumbs.dump_linked_data(project)
] %}
{% if linked_data %}
    <script type="application/ld+json">
        {{ linked_data | json | safe }}
    </script>
{% endif %}

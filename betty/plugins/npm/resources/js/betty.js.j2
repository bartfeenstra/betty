'use strict'

import style from './betty.css'

{% for entry_point_alias, import_path in plugins['betty.plugins.npm.Npm'].entry_points %}
    import { betty as {{ entry_point_alias }} } from "{{ import_path }}"
    document.addEventListener('DOMContentLoaded', {{ entry_point_alias }})
{% endfor %}

{%- import 'macro/date.html.j2' as dateMacros -%}
{%- import 'macro/place.html.j2' as placeMacros -%}
{% if event.date %}{{ dateMacros.format(event.date) }}{%- endif %} {% if event.place and event.place != place_context %}in {{ placeMacros.label(event.place, place_context) }}{% endif -%}
{% for reference in event.references %}
    {% set referenceNumber = references.use(reference) %}
    <a href="#reference-{{ referenceNumber }}" class="reference-usage">[{{ referenceNumber }}]</a>
{% endfor %}

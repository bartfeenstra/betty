{%- import 'macro/person.html.j2' as personMacros -%}
<ul class="entities">
    {%- for person in persons | rejectattr('name', 'none') | sort(attribute='name') %}
        <li class="{{ loop.cycle('odd', 'even') }}" {% if typeOf %} typeof="{{ typeOf }}"{% endif %} {% if property %}property="{{ property }}"{% endif %}>
            {{ personMacros.sortableLabel(person) }}
            {% include 'person-meta.html.j2' %}
        </li>
    {%- endfor -%}
</ul>

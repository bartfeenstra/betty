<figure>
    <img src="{{ file | image(1500, 1500) | static_url }}" class="image-fit"{% if file.description %} alt="{{ file.description }}" title="{{ file.description }}"{% endif %}>
    {% if file.description %}
        <figcaption>{{ file.description }}</figcaption>
    {% endif %}
</figure>

{{ {
    'debug': app.project.configuration.debug,
    'cacheDirectory': app.cache.with_scope(app.extensions['betty.extension.Trees'].name()).with_scope(app.project.id).with_scope('babel').path | str,
} | tojson }}

dist: xenial

language: python

python:
    - "3.5"
    - "3.6"
    - "3.7"

cache: pip

install:
    - npm --version
    - ./bin/build-dev

script:
    - ./bin/test

after_failure:
    - ls -la ~/.betty/*

after_success:
    - codecov
